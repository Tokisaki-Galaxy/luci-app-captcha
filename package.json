{
  "name": "luci-app-captcha",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LuCI CAPTCHA Authentication Plugin",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "lint": "echo 'No linting configured'",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "playwright": "^1.40.0",
    "prettier": "^3.1.0",
    "typescript": "^5.9.3",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
