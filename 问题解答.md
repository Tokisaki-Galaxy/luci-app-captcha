# GitHub Actions 构建问题解答

## 问题回答

### 问题1：为什么构建流程不支持版本号 < 1.0.0（如 0.0.1）？

**答案：完全支持！这可能是一个误解。**

#### 详细说明

我详细分析了 `.github/workflows/build.yml` 的版本处理逻辑，确认工作流**完全支持任何版本号格式**，包括：

- ✅ `v0.0.1` → 生成 `0.0.1-r1`
- ✅ `v0.1.0` → 生成 `0.1.0-r1`
- ✅ `v1.0.0` → 生成 `1.0.0-r1`
- ✅ `v2.3.4-beta` → 生成 `2.3.4-beta-r1`

版本提取使用的命令是：
```bash
NEW_VER=$(echo "${{ github.ref_name }}" | sed 's/^v//')
```

这个 `sed` 命令只是简单地去掉标签开头的 'v' 字符，对所有版本号都有效。

#### 测试验证

我编写了测试脚本验证版本提取逻辑，所有测试用例都通过：
```
✅ PASS: v0.0.1 -> 0.0.1
✅ PASS: v0.1.0 -> 0.1.0
✅ PASS: v1.0.0 -> 1.0.0
✅ PASS: v1.2.3 -> 1.2.3
✅ PASS: v2.0.0-beta -> 2.0.0-beta
```

#### 如何使用

创建小于 1.0.0 的版本标签：
```bash
git tag v0.0.1
git push origin v0.0.1
```

工作流会自动构建并生成 `luci-app-captcha_0.0.1-r1_all.ipk`。

---

### 问题2：为什么版本号为 v1.0.0 时会出现两个 IPK 文件？

**答案：这是一个缓存和清理问题，已修复。**

#### 问题表现

在同一次构建中出现两个不同版本的文件：
```
luci-app-captcha_1.0.1-r1_all.ipk              ← 标签版本
luci-app-captcha_2026.02.06-r41.git-xxx_all.ipk  ← 日期版本
```

#### 根本原因

经过深入分析，发现了以下问题：

1. **构建产物清理不彻底**
   - 旧代码使用 `find ./bin/packages -type f -name "*.ipk" -delete`
   - OpenWrt SDK 的 `./bin/packages` 有多层嵌套目录结构
   - 某些情况下旧文件可能未被完全清理

2. **SDK 缓存污染**
   - SDK 被缓存以加速构建
   - 缓存键不包含版本信息
   - 可能累积不同版本的构建产物

3. **缺少调试信息**
   - 无法看到版本注入是否成功
   - 无法看到构建了哪些文件

#### 解决方案

我已经实施了以下修复：

##### 1. 改进清理逻辑

**修改前：**
```bash
find ./bin/packages -type f -name "${PACKAGE_NAME}*.ipk" -delete
find ./bin/packages -type f -name "${PACKAGE_NAME}*.apk" -delete
```

**修改后：**
```bash
# 彻底清理旧的构建产物（包括所有子目录）
echo "Cleaning old build artifacts..."
rm -rf ./bin/packages/*
mkdir -p ./bin/packages
```

使用 `rm -rf ./bin/packages/*` 确保所有旧文件被完全删除。

##### 2. 添加调试日志

在构建流程的关键步骤添加了详细的日志输出：

**版本注入验证：**
```bash
echo "::group::Makefile after PKG_VERSION injection"
grep -A 2 "include.*rules.mk" "$TARGET_MK" || true
echo "::endgroup::"
```

**显示构建的文件：**
```bash
echo "::group::Built IPK files"
find ./bin/packages -type f -name "*.ipk" -ls
echo "::endgroup::"
```

**显示收集的包：**
```bash
echo "::group::Collected packages"
ls -lh upload/${REL_PATH}/*.ipk upload/${REL_PATH}/*.apk 2>/dev/null
echo "::endgroup::"
```

##### 3. 增强版本通知

添加了 GitHub Actions 通知，清晰显示正在构建的版本：

```bash
echo "::notice::Building release version $NEW_VER from tag ${{ github.ref_name }}"
```

或

```bash
echo "::notice::Building development snapshot version $NEW_VER-r$NEW_REL"
```

#### 如何验证修复

1. **查看 Actions 日志**
   - 在 GitHub Actions 运行日志中展开新增的日志组
   - 确认 "Makefile after PKG_VERSION injection" 显示了正确的版本号
   - 确认 "Built IPK files" 只显示一个版本的文件
   - 确认 "Collected packages" 只有预期的包

2. **测试新标签**
   ```bash
   git tag v1.0.0
   git push origin v1.0.0
   ```
   
   应该只生成一个文件：`luci-app-captcha_1.0.0-r1_all.ipk`

## 为什么其他项目没有这个问题？

可能的原因：

1. **其他项目在 Makefile 中定义了 PKG_VERSION**
   - 如果 Makefile 中已经有 `PKG_VERSION:=1.0.0`
   - 工作流会跳过动态注入，直接使用静态版本
   - 这样不会受到 luci.mk 的 `findrev` 函数影响

2. **其他项目的缓存状态不同**
   - 首次构建时不存在缓存
   - 或者缓存被正确清理

3. **其他项目的构建产物结构不同**
   - 可能没有复杂的嵌套目录结构

## 已修改的文件

1. **`.github/workflows/build.yml`**
   - 改进了构建产物清理逻辑
   - 添加了详细的调试日志
   - 增强了版本处理的注释和通知

2. **`BUILD_ACTION_ANALYSIS.md`**（新增）
   - 详细的问题分析文档
   - 技术原理说明
   - 测试建议

## 建议

### 立即测试

建议创建一个测试标签来验证修复效果：

```bash
# 测试版本 < 1.0.0
git tag v0.0.1
git push origin v0.0.1

# 或测试正常版本
git tag v1.0.0  
git push origin v1.0.0
```

然后检查：
1. GitHub Actions 的构建日志
2. Releases 页面的文件数量和名称
3. GitHub Pages 上的软件源中的包

### 监控改进

在接下来的几次构建中：
- 查看新增的调试日志输出
- 确认只生成了预期的包
- 如果问题仍然存在，调试日志会帮助我们进一步诊断

## 技术细节

如果你想了解更多技术细节，请查看：
- `BUILD_ACTION_ANALYSIS.md` - 完整的技术分析（中文）
- `.github/workflows/build.yml` - 改进后的工作流文件

## 总结

✅ **问题1已解答**：版本 < 1.0.0 完全支持，可以放心使用 v0.0.1 等标签

✅ **问题2已修复**：改进了清理逻辑并添加了调试日志，应该能解决双重文件问题

🔍 **需要验证**：建议创建测试标签验证修复效果，如有问题可通过新增的调试日志进一步诊断
